# 🔄 研发交接快照 (CURRENT.md / HANDOFF)
当前战役: C2V / K2V 鉴权穿透、载荷映射与历史阵列重构 (Auth Penetration, Payload Mapping & History Matrix Refactor)
宏观状态: C2V 与 K2V 视听解析车间已全面竣工 (100%)。
执行纪律: 严格遵循自底向上（状态机 -> 视图层 -> 适配器 -> 后端勘探）的原子化重构顺序，每一步必须独立测试闭环。

---

## 已完成 (Done)
- [x] **C2V 异构载荷精准映射**：剥离 `profile_text`，打通 `extra_info`，完美匹配 Pydantic Schema。
- [x] **K2V/C2V 鉴权穿透壁垒**：彻底废弃原生 `src` 裸连，建立 Blob 对象转换机制，解决 422 拦截。
- [x] **双端视图解封与同构**：砸碎 K2V 三元表达式覆盖逻辑，建立 16:9 沉浸播放器与底部 3 列点击回溯历史阵列。

## 极其详细的下一步开发计划 (Next Actionable Steps)

### 阶段一：动态视频时长自适应引擎 (AI-Driven Duration Estimation) 落地
- [ ] **目标 1：废除硬编码时长**。系统级移除 `duration: 5` 的固定策略，改为按输入复杂度动态估算。
- [ ] **目标 2：轻量级 LLM 预检路由**。在正式生成前增加一次预检请求，结合题目/知识点长度、结构复杂度、目标难度，返回推荐时长区间。
- [ ] **目标 3：映射与回退闭环**。将预检结果映射到 K2V/C2V 真实载荷；若预检失败，必须触发受控回退策略并记录日志，避免阻塞主生成链路。

### 阶段二：向 `/vault` 与 `/tools/3d-sandbox` 进军 (Module Expansion)
- [ ] **目标 1：`/vault` 个人金库基础架构搭建**。完成路由接入、容器骨架、接口目录与状态管理隔离，确保与 AIGC 车间零耦合。
- [ ] **目标 2：`/tools/3d-sandbox` 具身交互沙盒底座搭建**。建立独立视图容器、渲染循环占位、输入控制总线与最小可运行场景。
- [ ] **目标 3：新版块物理隔离与规范继承**。严格沿用 `PROJECT.md` 的 Layout、样式变量与 API 分层宪法，避免跨模块污染。
