// ================================================================
// API 前缀统一配置
// ================================================================

/** 统一的微服务前缀，由 Vite 代理映射至后端根路径 */
export const API_PREFIX = {
  GOAGENTS: '/api/goagents',
} as const;

